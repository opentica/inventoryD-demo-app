apiVersion: v1
kind: Service
metadata:
  name: inventoryservice
  namespace: default
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: inventoryservice
  type: LoadBalancer
